import 'package:flutter/material.dart';

/// Contains constants for styling the UI.

/// The style for the header title in login and registration.
const headerStyle = TextStyle(fontSize: 32, fontWeight: FontWeight.bold);

/// The padding around a widget in login and registration.
const contentHorizontalPadding = EdgeInsets.symmetric(horizontal: 20.0);