/// Contains enums for tracking the state of the application.

/// The application login state
enum ApplicationLoginState {
  loggedOut,
  loggedIn,
}

/// The application registration state
enum ApplicationRegisterState {
  registering,
  registered,
}